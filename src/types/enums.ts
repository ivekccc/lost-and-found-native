// ===========================================
// ENUMS - 1:1 SA BACKEND
// ===========================================

export enum AuthProvider {
  LOCAL = 'LOCAL',
  GOOGLE = 'GOOGLE',
}

export enum ListingType {
  LOST = 'LOST',
  FOUND = 'FOUND',
}

export enum ListingStatus {
  ACTIVE = 'ACTIVE',
  COMPLETED = 'COMPLETED',
}
